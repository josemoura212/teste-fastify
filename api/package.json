{
  "name": "fastify-api",
  "version": "1.0.0",
  "description": "API usando Fastify",
  "main": "dist/app.js",
  "scripts": {
    "dev": "tsx watch src/infrastructure/api/server.ts",
    "start": "node dist/infrastructure/api/server.js",
    "build": "tsc",
    "build:start": "npm run build && npm start"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "24.0.13",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}